[package]
name = "qbz-media-cache"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "One SQLite cache for every remote media server QBZ reads (Jellyfin, Subsonic, and Plex when it migrates)"

[dependencies]
rusqlite = { version = "0.40", features = ["bundled"] }
serde_json.workspace = true

[dev-dependencies]
tempfile = "3"
